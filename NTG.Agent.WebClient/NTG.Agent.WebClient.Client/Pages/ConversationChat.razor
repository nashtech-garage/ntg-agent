@page "/c/{id:guid}"
@rendermode @(new InteractiveWebAssemblyRenderMode(prerender: false))

<PageTitle>Chat</PageTitle>

<ChatManager ConversationId="Id" Language="Language" 
             Experience="default"></ChatManager>

@code {
    [Parameter]
    public Guid Id { get; set; }
}
